import { AppointmentValidator } from './appointment-validator';

describe('AppointmentValidator', () => {
  it('should create an instance', () => {
    expect(new AppointmentValidator()).toBeTruthy();
  });
});
